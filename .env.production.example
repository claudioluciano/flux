# Production Environment Variables
# These are configured in Vercel Dashboard → Settings → Environment Variables (Production scope)
# and in Convex Dashboard → Settings → Environment Variables (prod deployment)

# Vercel Environment Variables (Production scope)
CONVEX_DEPLOYMENT=prod:astute-pika-659
NEXT_PUBLIC_CONVEX_URL=https://astute-pika-659.convex.cloud
NEXT_PUBLIC_CONVEX_SITE_URL=https://astute-pika-659.convex.site
NEXT_PUBLIC_SITE_URL=https://flux.sheltech.app
BETTER_AUTH_SECRET=<generate-secure-secret>

# Convex Environment Variables (prod deployment)
# Set via: npx convex env set <NAME> <VALUE> --prod
SITE_URL=https://flux.sheltech.app
BETTER_AUTH_SECRET=<same-as-vercel>
